version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Starting deployment"
        build:
          commands:
            - echo "Static site ready"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
      cache:
        paths: []